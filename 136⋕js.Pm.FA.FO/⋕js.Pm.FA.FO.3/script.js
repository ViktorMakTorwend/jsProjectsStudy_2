/*⋕js.Pm.FA.FO.3*/

let obj = {
    sumEl: function(num1, num2) {return num1 + num2},
    sumPow: function(num1, num2) {return Math.pow(num1, 2) + Math.pow(num2, 2)},
    sumCub: function(num1, num2) {return Math.pow(num1, 3) + Math.pow(num2, 3)},
};

alert(obj.sumEl(2,3));
alert(obj.sumPow(2,3));
alert(obj.sumCub(2,3));

